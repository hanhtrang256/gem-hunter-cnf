## Library
Install PySAT to solve Conjunctive Normal Form problem.
Open cmd/terminal and type `pip install python-sat`

## Execute
Before executing, make sure the directory is at the src folder!
`python main.py`